@using Microsoft.IdentityModel.Tokens;
@model ExpenseTrack.Models.UserProfile.UserProfileViewModel

@{
    ViewData["Title"] = "User Profile";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h2 class="text-center">User Profile</h2>

            <form asp-controller="UserProfile" asp-action="UpdateProfile" class="mt-4" method="post" enctype="multipart/form-data">

                <div class="text-center mb-3">
                    <img src="@(!string.IsNullOrEmpty(Model.UserProfilePicture) ? Model.UserProfilePicture : "/images/profilepic.jpg")" class="rounded-circle" style="width: 150px; height:150px; margin-bottom: 10px;" alt="Avatar" />
                    <input type="file" class="form-control" name="PictureFile" accept="image/*">
                </div>
                <div class="form-group">
                    <label>Email</label>
                    <input type="text" class="form-control" value="@((Model != null && !string.IsNullOrEmpty(Model.Email)) ? Model.Email : string.Empty)" disabled>
                </div>

                <div class="form-group">
                    <label>First Name</label>
                    <input type="text" class="form-control" name="FirstName" value="@((Model != null && !string.IsNullOrEmpty(Model.FirstName)) ? Model.FirstName : string.Empty)" required>
                </div>

                <div class="form-group">
                    <label>Last Name</label>
                    <input type="text" class="form-control" name="LastName" value="@((Model != null && !string.IsNullOrEmpty(Model.LastName)) ? Model.LastName : string.Empty)" required>
                </div>

                <div class="form-group">
                    <label>Income</label>
                    <input type="number" class="form-control" name="Income" value="@((Model != null) ? Model.Income.ToString() : string.Empty)" min="1" pattern="\d+" title="Please enter a valid number" required>
                </div>


                <!-- Add similar checks for other properties -->

                <div class="text-center mt-3">
                    <button type="submit" class="btn btn-primary">Update Profile</button>
                </div>

            </form>
        </div>
    </div>
</div>





@* <div class="text-center mb-3">
    <img src="~/images/pp.jpg" class="rounded-circle" style="width: 150px; height:150px; margin-bottom: 10px;"
         alt="Avatar" />
    <input type="file" class="form-control" name="ProfilePictureFile" accept="image/*">
</div> *@